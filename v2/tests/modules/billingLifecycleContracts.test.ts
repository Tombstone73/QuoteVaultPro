import assert from "node:assert/strict";
import { capabilityIds } from "../../src/authorization/capabilities.js";
import type { IssuedInvoiceBoundary, IssuedInvoiceCheckpoint } from "../../src/modules/billing/contracts.js";
import { brandedId, currencyCode, money } from "../../src/modules/shared/commercialValues.js";
const usd=currencyCode("USD"),boundary:IssuedInvoiceBoundary={invoiceId:brandedId<"InvoiceId">("invoice"),status:"issued",checkpointId:brandedId<"InvoiceCheckpointId">("checkpoint"),silentOrderSynchronization:false};
const checkpoint:IssuedInvoiceCheckpoint={schemaVersion:1,checkpointId:boundary.checkpointId,evidenceFingerprint:"sha256:fixture",invoiceId:boundary.invoiceId,organizationId:brandedId<"OrganizationId">("org"),occurredAt:"2026-08-16T00:00:00.000Z",principal:{principalKind:"staff",subjectId:"staff"},customerPresentation:{customerDisplayName:"Customer"},commercial:{currency:usd,subtotal:money(usd,100),taxTotal:money(usd,0),total:money(usd,100)},taxEvidence:{calculationId:"fixture",calculatorVersion:"zero",components:[]},lines:[{lineId:brandedId<"SalesLineId">("line"),productId:brandedId<"ProductId">("product"),description:"Fixture",quantity:1,unitAmount:money(usd,100),lineAmount:money(usd,100),salesPricingEvidenceFingerprint:"sha256:price"}]};
assert.equal(boundary.silentOrderSynchronization,false,"issuance explicitly disables silent Sales synchronization");assert.equal(checkpoint.commercial.subtotal.cents+checkpoint.commercial.taxTotal.cents,checkpoint.commercial.total.cents,"issued money stays integer-cent exact");assert.equal(checkpoint.lines[0]?.lineAmount.cents,checkpoint.commercial.total.cents,"issued line snapshot preserves commercial truth");assert.equal("paymentState" in checkpoint,false,"issued is not a payment ledger");assert.equal("deliveryState" in checkpoint,false,"issuance is not delivery");assert(capabilityIds.includes("invoice.issue"),"invoice issuance uses reviewed authority vocabulary");console.log("[m3.3] Billing lifecycle contract tests passed (6 assertions).");
