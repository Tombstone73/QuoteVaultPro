# Current Lovable presentation reference

`reference/lovable-ui` is the **current Lovable presentation authority** for
PrintersHero / TitanOS V2. It is comparison source only: it is not imported by,
built with, or deployed as the active V2 application.

## Source

- Supplied export: `PrintFlow Command (7).zip`
- Imported: 2026-09-07
- Archive SHA-256:
  `d7f19e282abf5f47b3d55250df2ef53eb0b4e07f3ed4ac8a40e579290e65eb23`
- Lovable project template/revision:
  `tanstack_start_ts_current` /
  `tanstack_start_ts_current-c5239f3e3f32`

The previous `reference/lovable-ui` snapshot was superseded in place. It is
intentionally retained only through Git history; it is no longer current
presentation authority. Historical audits that name this path describe the
reference available when they were written, not the new export's behavior.

Future UI work may compare against this source only after a separately approved
V2 integration milestone. It must not copy mock behavior, data, providers, or
workflow authority into V2 without canonical domain and API ownership.
