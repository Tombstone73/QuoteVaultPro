# Architecture Findings Register

| ID | Domain | Severity | Finding | Evidence | Consequence | Recommendation |
| --- | --- | --- | --- | --- | --- | --- |
| ARCH-001 | Orders / authorization | P0 | Cancellation bypasses avoid canonical cleanup. | `orderCancellationService.ts:478-904` is atomic; `orders.routes.ts:3045-3208`, `3535-3571`, `1394-1455` permit alternative cancelled state writes. | Active jobs, proofs, invoices/payments, fulfillment, reservations may survive a cancelled order. | Route every cancelled terminal action through one authorization-protected cancellation operation; test every entry point. |
| ARCH-002 | Inbound / billing | P0 | Inbound order creation hard-codes zero tax. | `InboundOrderService.ts:5767-5770,5797-5799`; invoice derives persisted order totals in `orders.repo.ts:1669-1674`. | Taxable inbound orders can underbill and understate A/R. | Reuse the canonical tax calculation/snapshot path before order persistence. |
| ARCH-003 | Orders / billing | P0 | Order-line edits preserve stale tax and sync it to draft invoices. | `orders.routes.ts:336-383,7338,7459,8002,8773`; invoice sync `invoicesService.ts:501-523,761-831`. | Price/quantity/taxability changes can cause wrong invoices. | Make recalculation and draft-invoice synchronization one transactional application operation. |
| ARCH-004 | Catalog / authorization | P0 | PBV2 draft save lacks role authorization and can auto-activate. | `products.routes.ts:872,950,993-1091`; UI role value unused at `PBV2ProductBuilderSectionV2.tsx:360`. | Member can alter active product/pricing configuration. | Enforce org-scoped capability in the operation; route auto-activation through canonical publish. |
| ARCH-005 | Catalog / tenant isolation | P0 | Legacy product option/variant operations lack tenant ownership checks. | `products.routes.ts:2331-2435`; `storage/shared.repo.ts:615-693`; schema parent-only relation at `shared/schema.ts:1457,1686`. | Cross-tenant read or globally-admin mutation by guessed IDs. | Require tenant context, verify parent product organization for every operation, add isolation tests. |
| ARCH-006 | Authorization | P0 | Global `isAdmin` controls organization catalog/pricing routes despite membership being authoritative. | `routes.ts:125-154`; `localAuth.ts:706-717`; `products.routes.ts:1133,1915`; `pricing.routes.ts:88`. | A global-admin identity can exceed its active-org role. | Replace with one tenant-role capability middleware; retain global flags only for platform controls. |
| ARCH-007 | Fulfillment / billing | P0 | Ship/pickup commits before billing automation and retries cannot recover. | `fulfillment/repository.ts:438-556,757-909`; `fulfillment/service.ts:390-430,530-557,652-667`. | Shipped/picked-up orders may lack required invoices. | Transactional or durable idempotent post-commit reconciliation/outbox with retry. |
| ARCH-008 | Shipping / tenant isolation | P0 | Shipping email route is unscoped and calls service with shifted arguments. | `fulfillment.routes.ts:521-530`; `fulfillmentService.ts:111-208`. | Broken email; possible cross-tenant data read/email. | Disable/replace with tenant-scoped canonical operation and behavioral tests. |
| ARCH-009 | Production / fulfillment | P0 | Job reopen leaves completed fulfillment/line/order state intact. | `productionJobs.routes.ts:4037-4097` versus safe undo `3824-3975`. | Contradictory work ownership and fulfillment release. | Restrict/replace with a canonical recovery that unwinds every successor. |
| ARCH-010 | Artwork | P0 | Attachment deletion leaves canonical artwork current. | `orderLineItemFiles.routes.ts:1181-1320`; canonical resolver `LineItemArtworkReadResolver.ts:73-110`. | Deleted artwork can still feed proofing, production, fulfillment. | Canonical detach/supersede plus projection cleanup in one transaction. |
| ARCH-011 | Payments | P0 | Stripe/reversal writes and invoice rollups are separate commits. | `mvpInvoicing.routes.ts:912-926,1246-1265,1358-1553,2612-2633`. | Payment exists while invoice balance/status remains stale; retry hole. | Centralize transactional/reconcilable payment finalization; replace delete with audited reversal. |
| ARCH-012 | Credit | P0 | Credit ledger, `currentBalance`, and invoice exposure disagree; legacy write likely violates active schema. | `customers.repo.ts:1123-1132`; `customerRelations.routes.ts:543-609`; `customerCreditExposureService.ts:11-42`. | Failed/hidden credit or wrong exposure. | Select one financial source of truth and migrate projections deliberately. |
| ARCH-013 | Invoices | P0 | Invoice PATCH directly changes header totals without immutable-line synchronization. | `mvpInvoicing.routes.ts:2297-2370`; canonical safe patch `invoicesService.ts:883-910`. | Header/PDF and invoice-line snapshots can disagree. | Make financial edits canonical/transactional or limit endpoint to safe fields. |
| ARCH-014 | Production | P1 | Order-level complete-production bypasses job/run outcomes. | `orders.routes.ts:3217-3292`; `productionJobs.routes.ts:346-550`; `productionRunService.ts:1381-1507`. | Stranded material/run quantities and premature fulfillment. | Restrict and reconcile/block active jobs/runs via one completion operation. |
| ARCH-015 | Prepress | P1 | Prepress transaction is porous because final-art service/timeline use global DB. | `prepress.routes.ts:2826-3037`; `prepressFileService.ts:1886-1983`. | Artwork/timeline can commit without session/workflow handoff. | Thread transaction context end-to-end; inject failure contracts. |
| ARCH-016 | Orders / billing | P1 | Order post-create and updates have non-atomic secondary initialization. | `orders.routes.ts:2456-2510`; `canonicalOrderOperations.ts:28-41`. | Order succeeds while routing/artwork/invoice becomes stale. | Add initialization state/retry record and transaction where feasible. |
| ARCH-017 | Orders | P1 | Direct order idempotency is process-local. | `orderCreationIdempotency.helpers.ts:1-55`. | Duplicate order/invoice on restart or multiple instances. | Persist a caller/request idempotency key with unique constraint. |
| ARCH-018 | Products / PBV2 | P1 | Route-local auto-publication duplicates canonical publication. | `products.routes.ts:993-1091` vs `canonicalProductPublishOperations.ts:47-59`. | Validation/audit/stale-write semantics can drift. | Delegate auto-save activation to canonical publish and parity-test it. |
| ARCH-019 | Products | P1 | Parallel options/tree/version/legacy option/variant representations have competing mutations. | `shared/schema.ts:745-905,1457,1686`; product route/import/AI paths. | Product configuration can diverge by caller. | Define supported representation contract; strangler-migrate legacy paths. |
| ARCH-020 | Artwork / production | P1 | Artwork side/allocation/delete are separate mutation owners. | `orderLineItemFiles.routes.ts:880-1166`; `artworkSetOperations.ts:102-166`. | Canonical and compatibility projections drift. | Complete the artwork command surface before new integrations. |
| ARCH-021 | Production / prepress | P1 | Return-to-prepress and completion/readiness are duplicated. | `productionReturnToPrepressService.ts:34-155`; `prepress.routes.ts:3426-3599`; `productionRunService.ts:2091-2315`. | Workflow changes break one recovery path but not another. | One shared ordinary recovery/completion operation; retain run-specific constraints. |
| ARCH-022 | Proofing | P1 | Proof state commits before fire-and-forget email. | `proofing.routes.ts:611-745`. | Line can block awaiting an undelivered proof. | Durable notification status/outbox and visible recovery. |
| ARCH-023 | Fulfillment | P1 | Event/audit writes often occur after state changes. | `fulfillment/service.ts:249-387`; `repository.ts:1417-1474`. | Successful mutation can return failure/no audit; retries duplicate effects. | Include event/audit in operation transaction or durable outbox. |
| ARCH-024 | Authorization | P1 | Finance authorization differs by route. | `mvpInvoicing.routes.ts:533,843,1112,1198,1750,1808,2189,2228,2584,2613` vs provider permission gate. | Sensitive action access is hard to reason about and may be overbroad. | Migrate finance mutations to shared org capability policy. |
| ARCH-025 | Frontend / pricing | P1 | Shared document form computes option costs/totals/default tax locally. | `DocumentCreateForm.tsx:158-386`. | UI preview/submission can drift from authoritative pricing/tax. | Use server pricing/tax response as display authority; retain client validation only. |
| ARCH-026 | Billing / customers | P1 | Customer merge directly rewrites many foreign domains without callbacks. | `customerCanonicalIdentityService.ts:165-446`. | Future projections/automation can be missed. | Keep transaction but publish internal merge record/contract. |
| ARCH-027 | Pricing | P2 | Advanced PBV2 override is route-owned configuration. | `products.routes.ts:1238-1397`; evaluator `PricingService.ts:674-690`. | Future audit/concurrency rules can diverge. | Named canonical override operation before expansion. |
| ARCH-028 | Pricing | P2 | Formula/template CRUD is route/storage-owned. | `pricing.routes.ts:88-229`. | Inherits inconsistent auth/audit patterns. | Canonical pricing-admin operation after authorization consolidation. |
| ARCH-029 | Fulfillment | P2 | Duplicate status route and legacy UI call blocked shipment APIs. | `orders.routes.ts:5884-5923`; `fulfillment.routes.ts:451-518`; `useShipments.ts:19-89`. | Confusing dead surface; future accidental re-enable risk. | Remove after compatibility validation. |
| ARCH-030 | Architecture | P3 | Large route/service files concentrate rules. | Orders 8,565 lines; inbound 6,024; pricing 5,042; production jobs 4,146. | Review/change blast radius is high. | Split only while extracting real canonical operations, not as formatting work. |
